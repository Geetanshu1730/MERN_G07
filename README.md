# MERN_G07
This is Mern repo.


1..10 | ForEach-Object { $folder = "day_{0:D2}" -f $_ mkdir $folder "## $folder" | Out-File "$folder/README.md" }


1. git add. --> this will stage the changes
2. git commit -m "Day 1 added with html" (for comments) --> save changes here
3. git push origin main --> go to cloud
